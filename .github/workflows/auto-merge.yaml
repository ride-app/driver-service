name: Auto Merge Bots

on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read
  pull-requests: write

jobs:
  comment_pr_if_bot:
    name: Comment on pull request if bot
    uses: ride-app/workflows/.github/workflows/auto-merge.yaml@main
